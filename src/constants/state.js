/**
 * Const identifying the player's state.
 *
 * Makes it easier to track the UI when the state is clear.
 */
const PLAYER_STATE = {
    PLAYING: 'PLAYING',
    PAUSED: 'PAUSED',
    BUFFERING: 'BUFFERING',
    ENDED: 'ENDED',
    ENDING: 'ENDING'
};

export {PLAYER_STATE};